{
  "name": "ramify.ts",
  "version": "0.0.0",
  "private": true,
  "description": "Toolkit to define, enforce, visualize, and help agents adhere to the ramify cross-module importability model.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
