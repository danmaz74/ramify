// This fixture is read through its own bundler-resolution configuration.
// @ts-ignore The independent scripts configuration intentionally has no @probe alias.
import { Shape as PathAliasShape } from '@probe/originals.js';

export type AliasUse = PathAliasShape;
