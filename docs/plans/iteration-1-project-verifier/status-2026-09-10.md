# Plan 1 status, 2026-09-10

Status summary for [Plan 1: Verify a real Ramify project](main-plan.md) at
commit `e048d51` on branch `workflow/iteration-1-project-verifier`.

## Workflow

- The cucumber-viz workflow `_JG0Ucx92X1f6WqRyuxxC` has all 15 iterations
  completed and accepted and sits in end-of-workflow review.
- The branch is audited at `e048d51`: the full toolkit Vitest suite and the
  type-check pass. The worktree carries the uncommitted catalog fix described
  below and this status file. The branch is not merged to `main`.
- The gap-analysis stage has not run.

## Findings

33 findings in total.

| Group | Count | State |
| --- | --- | --- |
| Waived through the workflow MCP surface | 2 | The type-only erasure case (Ramify does not type-check; the compiler rejects the input) and the iteration 15 self-assessment (superseded by the audit). |
| Risk findings fixed in `e048d51` | 4 | Harness fixture path, CLI exit 130 window, `createRequire` loaders, shared-global coverage notes. Still decision-pending in Studio until the risk review is rerun or they are decided in the review UI. |
| Constraint contradictions from iterations 5–13 | 27 | All 27 are fixed in code with passing regression tests. The last one, `cf-constraints-mtu4u21y-erbr5cib`, is fixed in the uncommitted working tree: `subs/analysis/subs/typescript/src/catalog.ts` now registers a named selection that receives a module namespace as a namespace target, so a forwarder of an incomplete or ambiguous namespace becomes incomplete with a located `incomplete-exports` note, and star forwarders of it follow. Three regressions in `catalog.test.ts` cover ambiguous and unresolved constituents and three forwarding hops. No new coverage code or message. All 27 still show remediation-required in Studio. |

## Implementation

Complete. The batch engine, the compiled CLI with both invocation forms, all
ten validation scripts, and the reference harness with 308 reviewed instances
and 308 registered providers exist. The unchanged reference checks clean and
the self-check reports nine owners with no analysis limits, on the previous
build.

## Completion gate

- `e048d51` was built and `npm run reference:verify -- --plan 1` ran on it at
  19:42 on 2026-09-10. Result: passed, plan complete. All 308 instances were
  required, executed and passed, including the three iteration 15 instances and
  the relocated package's own test suite. The run took about 17 minutes.
- The portable report is
  `.reference-work/reports/plan1-full-9e4f25e1-a5de-4f60-9de9-46a2de183fb5.json`,
  an ignored execution artifact. Its identity records revision `e048d51` with a
  dirty tree; the only difference from the commit is this untracked status
  file.
- The retained measurement, diagram and site results still predate today's
  runtime changes, so they are not tied to `e048d51`.
- The [iteration 15 results](iterations/iteration15-results.md) still decline
  to claim completion and need this artifact recorded.

## Validation sequence

Run on the built `e048d51` on 2026-09-10 before the catalog fix was applied;
every command exited 0. Logs are `.reference-work/final-<name>.log`.

| Command | Result |
| --- | --- |
| `npm run check:reference` | Two configuration-file warnings, no errors, no analysis limits. |
| `npm run check:self` | Nine owners, no findings, no analysis limits. |
| `npm run diagrams` | Nine SVGs written; no tracked file changed. |
| `npm run site:build` | Client and server compiled without warnings. |
| `npm run reference:cases` | 16 files, 241 tests passed. |
| `npm run reference:report` | Passed; report `.reference-work/reports/plan1-full-e1c7428b-cf0e-4cf7-bea6-448947cf9c73.json`. |
| `git diff --check` | Clean. |

The catalog fix was then applied and verified with the catalog tests (23
passed), the analysis package tests (108 and 147 passed), the type-check, and a
rebuilt self-check and reference check that report the same results as above.

## Known follow-ups from today's fixes

- A `require` target that is a module file is recorded with coverage notes
  but is not resolved, so no testing-origin denial fires for it. Bare `require`
  has the same limit; changing it is a resolution design decision.
- `declare global` blocks inside module files still pass silently. The model
  treats them as unverifiable, so they should receive the same shared-global
  coverage note as scripts.

## What is left, in order

1. Audit the working tree so the catalog fix becomes a commit with bound
   evidence, then rerun the completion gate on that commit, about 17 minutes,
   so a single revision carries the gate, validation and audit results.
2. Remeasure with `npm run measure:batch` on that build, about seven minutes,
   and archive the results as the measurements README describes.
3. Dispose of the six open Studio decisions: rerun the risk review so the four
   fixed findings reconcile as absent, or decide them in the review UI. Then let
   the gap-analysis stage run.
4. Record the two follow-ups above as Plan 2 inputs, update the iteration 15
   completion report and checklist through the workflow tools with the new
   artifacts, then merge the workflow branch to `main` and move Plan 1 to
   complete in the [roadmap](../tooling-architecture/README.md).

## Housekeeping on `main`

The plan-review item file has an uncommitted edit, and two analysis directories
under `docs/analysis/` plus the devcontainer lock file are untracked.
