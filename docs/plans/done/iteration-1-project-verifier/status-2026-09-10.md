# Plan 1 status, 2026-09-10

Final status of [Plan 1: Verify a real Ramify project](main-plan.md), merged
to `main` on 2026-09-10. Plan 1 is complete.

## Workflow

- The cucumber-viz workflow `_JG0Ucx92X1f6WqRyuxxC` completed all 15
  iterations, finished its end-of-workflow review after a server restart
  released a stalled finalize step, and merged
  `workflow/iteration-1-project-verifier` into `main` as `5b943e1`.
- Studio then moved the plan to `docs/plans/done/`; `f5b0939` committed that
  relocation and repointed every reference. The workflow worktree, the
  plan-review worktree and both branches were removed afterwards.
- The gap-analysis stage did not run; the merge audit was skipped by Studio.
  The gate, validation sequence and measurement below stand in for both.

## Findings

33 findings in total.

| Group | Count | State |
| --- | --- | --- |
| Waived through the workflow MCP surface | 6 | The type-only erasure case, the iteration 15 self-assessment, and the four risk findings after they were fixed in `e048d51`: harness fixture path, CLI exit 130 window, `createRequire` loaders, shared-global coverage notes. |
| Constraint contradictions from iterations 5–13 | 27 | All 27 fixed in code with passing regression tests. The last, `cf-constraints-mtu4u21y-erbr5cib`, was fixed in `581f2f3`: a named selection that receives a module namespace is registered as a namespace target, so forwarders of an incomplete or ambiguous namespace become incomplete with a located `incomplete-exports` note. Studio's record keeps all 27 at remediation-required because its review phase never offered a waiver for them. |

## Implementation

Complete and merged. The batch engine, the compiled CLI with both invocation
forms, all ten validation scripts, and the reference harness with 308 reviewed
instances and 308 registered providers are on `main`. The unchanged reference
checks clean and the self-check reports nine owners and 144 source files with
no findings and no analysis limits.

## Completion gate on `main`

`npm run reference:verify -- --plan 1` at `f5b0939` on a clean tree
ran from 21:19:02 to 21:34:04 UTC and took 15.0 minutes. Result: passed,
plan complete; 308 required, 308 passed, 0 failed, 0 not executed. Source
SHA `5271fb2e34c4f4a6209d574f9242e681b085ca7529f56ca16c2f1322c461ddad`, build
SHA `a2a0148124f14bad145e8779f0be66731ab3e6cdd83ebd41ea4b0b110f9c91b4`. The portable
report is archived as
[plan1-complete.json.gz](../../../../scripts/reference-harness/evidence/plan1-complete.json.gz).

## Validation sequence

Run on the built `e048d51` on 2026-09-10; every command exited 0.

| Command | Result |
| --- | --- |
| `npm run check:reference` | Two configuration-file warnings, no errors, no analysis limits. |
| `npm run check:self` | Nine owners, no findings, no analysis limits. |
| `npm run diagrams` | Nine SVGs written; no tracked file changed. |
| `npm run site:build` | Client and server compiled without warnings. |
| `npm run reference:cases` | 16 files, 241 tests passed. |
| `npm run reference:report` | Passed; 0 violations, all four reference tiers passed, 308 instances passed. |
| `git diff --check` | Clean. |

The catalog fix in `581f2f3` was verified afterwards with the catalog tests,
the analysis package tests, the type-check, a rebuilt self-check and reference
check, and the audit's full Vitest run.

## Batch measurement on `main`

`node scripts/measurements/run.mjs` on the gate's build, 21:34:16 to
21:40:35 UTC: passed every reviewed budget. Reference cold median
3.648 s, peak 480.863 MiB, RSS growth 21.594 MiB; 100 owners cold
median 6.130 s, peak 367.785 MiB, RSS growth 62.270 MiB. Archived as
[merged-main.json.gz](../../../../scripts/measurements/results/merged-main.json.gz) with its
hashes in the measurement index and summary.

## Inputs carried to Plan 2

Recorded in the [roadmap](../../tooling-architecture/README.md) and the
[iteration 15 results](iterations/iteration15-results.md):

- A `require` target that is a module file is recorded with coverage notes
  but is not resolved, so no testing-origin denial fires for it. Bare `require`
  shares that limit; resolving it is a resolution design decision.
- `declare global` blocks inside module files pass silently. The model treats
  them as unverifiable, so they should receive the same shared-global coverage
  note that script source receives.

## What is left

Nothing for Plan 1. The iteration 15 checklist keeps its historical
`allNewTestsPass: false`, recorded under Studio's no-local-tests policy before
the audits ran the full suite. Plan 2's detailed plan is the next deliverable.
